appId: ${MAESTRO_APP_ID}
---
# iOS may show the native password-save sheet after Maestro types into secure
# fields. Dismiss it when present so app-level assertions inspect UNIT, not iOS.
- runFlow:
    when:
      visible: "Save Password?"
    commands:
      - tapOn: "Not Now"
      - waitForAnimationToEnd
