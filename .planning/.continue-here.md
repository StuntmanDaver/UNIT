---
context: default
phase: 05-qa-handoff
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-05-02T00:26:47Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

<current_state>
Mid QA-03 Section 1 iOS simulator UAT. All code fixes from this session are committed.
Simulator: iPhone 16 Pro Max, Metro running, dev build installed.
Shake simulator → Reload to pick up latest bundle before testing.
</current_state>

<completed_work>

This session:
- fix(profile): useCurrentUser 10s AbortController timeout — commit 6505d2e
- fix(admin): Account & Settings card for admin logout — commit f562857
- fix(profile): tenant Log Out always visible outside loading gate — commit cff8ff7
- fix(auth): scope:local signout prevents autoRefresh race — commit 7f11a2a
- fix(profile): requestAnimationFrame Alert defer — commit fea1ea8
- test(e2e): Maestro suite (unit/maestro/) with 10 flows + 3 subflows — commits ed5c5ad, 5a8be74, ed13141, 34b3c58
- fix(auth): AuthGuard redirects to login from onboarding/reset-password on signout — commit 4e0519c
</completed_work>

<remaining_work>

Section 1 — iOS simulator batch:
- Block B (admin): CSV staging → admin-01-02 CSV import → ADMIN-03 tenant management → M2-06 admin promotion review
- Block C (tenant): M1-05 fresh signup, M2-01 profile edit, M2-02 directory, M2-03 business detail, M2-04 create promotion, M2-07 local deals
- Block D: QA-04 full runtime sweep (run `npm run maestro test maestro/flows/qa-04-full-sweep.yaml`)

Section 2 (user): Vercel KNOWN-BUG-06 env re-entry — recipe in .planning/phases/03-m2-core-mvp-audit/followups/M2-05-production-verify.md

Section 3 (after Section 2): Production Stripe test cycle

Section 4: EAS dev build + device push UAT (NOTIF-01..05)
</remaining_work>

<decisions_made>

- useCurrentUser: AbortController with 10s timeout, signal threaded through businessesService.filter(.abortSignal()). Root cause: Supabase JS auth-token refresh can hang indefinitely.
- Maestro at unit/maestro/ (not .maestro/) — co-located with app.
- admin-01-02-csv-import.yaml is SEMI-AUTOMATED: native iOS Files picker cannot be Maestro-driven; CSV staging is manual.
- AuthGuard fix: changed !inAuthGroup guard to explicit !onLogin && !onSignup check so signout from onboarding/reset-password routes to login correctly.
</decisions_made>

<blockers>

- CSV staging into Simulator Files app: manual. Run `cd /tmp && python3 -m http.server 9292`, open http://127.0.0.1:9292/m1-test-tenants.csv in Simulator Safari → Share → Save to Files.
- Section 2 Vercel env fix: user-owned dashboard action.
</blockers>

## Required Reading
1. `.planning/phases/05-qa-handoff/followups/QA-03-master-uat-rollup.md` — full Section 1 checklist
2. `unit/maestro/flows/` — 10 automated flows ready to run
3. `.planning/HANDOFF.json` — structured state (machine-readable)

## Infrastructure State
- Maestro 2.4.0: `~/.maestro/bin/maestro` — requires `JAVA_HOME=/opt/homebrew/opt/openjdk@21`
- npm script: `npm run test:e2e` runs all flows; `npm run maestro test <flow>` runs one
- Metro: should be running (`npx expo start --ios` from unit/)
- Simulator: iPhone 16 Pro Max — shake → Reload to pick up latest JS bundle

<context>
All blocking code issues for Section 1 UAT are resolved. The Maestro suite is the new accelerator — run qa-04-full-sweep.yaml first as a smoke test to confirm basic navigation works, then use the individual flows to drive the UAT items. CSV-dependent tests (admin-01-02) still need the manual staging step.
</context>

<next_action>
1. Shake simulator → Reload (picks up useCurrentUser timeout fix + AuthGuard fix)
2. `cd unit && npm run maestro test maestro/flows/qa-04-full-sweep.yaml` — smoke test
3. Stage test CSV (python3 http server → Simulator Safari → Save to Files)
4. Run Block B admin UAT using the consolidated checklist in QA-03-master-uat-rollup.md
</next_action>
