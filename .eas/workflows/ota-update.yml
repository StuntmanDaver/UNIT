name: OTA Update on Main

on:
  push:
    branches:
      - main

jobs:
  update:
    name: Publish OTA Update
    type: update
    params:
      channel: production
      message: ${{ github.event.head_commit.message }}
