# Ralph Progress Log
Started: Sat May  2 21:38:29 EDT 2026
---

## Codebase Patterns
- `brand-lint` is an inline bash one-liner in `unit/package.json` `scripts.brand-lint` — a DENY-list (greps for banned `bg-white|bg-gray-|text-gray-|border-gray-|text-black|bg-black|font-bold|font-medium|font-nunito-bold|font-lora-bold|text-xs|text-lg|text-xl|text-4xl`). Adding new positively-named brand tokens to `tailwind.config.js` does NOT trip it.
- There is NO `npm run typecheck`. Use `npx tsc --noEmit` from `unit/`.
- This project root is `UNIT-PRoject/` (no trailing space). The git repo lives at `unit/.git`, NOT at the parent — run git commands from `unit/` or use `git -C unit`.
- Tailwind brand color tokens live in `unit/tailwind.config.js` under `theme.extend.colors.brand`. Class names are auto-derived (e.g. `cloud` → `bg-brand-cloud`, `'ink-muted'` → `text-brand-ink-muted`).
- Brand contract is documented in `unit/CLAUDE.md` "Brand Theming" section — keep it in sync with `tailwind.config.js` whenever palette/typography changes.
- When extending DB-row types in `unit/services/*.ts`, also update the local `*_COLUMNS` constant in the same file (e.g. `PROPERTY_COLUMNS` in `properties.ts`). Forgetting this means new fields silently come back undefined at runtime even though TS thinks they exist.
- When adding required fields to a row type used by a service `create()`/`update()` Omit signature, audit every caller — TS will catch them but the fix is to pass an explicit `null` for nullable DB columns rather than loosen the Omit (keeps the schema honest at the boundary).
- Discriminator helpers for sum-typed rows (e.g. `PromotionKind`, `getPromotionKind(p)`) belong as exported functions next to the row type, NOT as an extra field on the row type — Supabase `select('*')` won't populate computed fields and adding them to the type creates lying shapes.
- External-API helpers that are called from both UI hot paths and batch scripts (e.g. `services/geocoding.ts`) should serialize through a process-local throttle (single `lastRequestAt` timestamp + setTimeout wait) and NEVER throw — return `null` and `console.warn` so callers can store NULL and retry later.
- Standalone Node scripts under `unit/scripts/` cannot import `services/supabase.ts` because that pulls in `@react-native-async-storage/async-storage`. Construct a fresh `createClient(url, SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } })` inside the script. Service-role key is required for any RLS-bypassing batch update.
- Jest tests for services that issue both a `.eq().single()` query AND a list `.select()` on the same table need to mock `select()` per-call: `mockImplementationOnce(() => mockChain)` for the chained call, then `mockImplementationOnce(() => Promise.resolve({data, error}))` for the awaited list call. The list path resolves directly off `select()` because there is no `.single()`.
- For pure-math helpers (haversine, geometry, etc.) export the function alongside the service that uses it so tests can exercise the math directly without hitting the supabase mock at all. Kept `haversineMiles` exported even though only `getNearbyPropertyIds` consumes it internally — makes Daytona-cluster math testable as 4 trivial assertions.
- For aggregator services that issue multiple parallel queries on different tables (e.g. activity feed across promotions/posts/businesses/notifications), prefer a per-table thenable mock keyed off the `from(table)` argument: a single `tableData: Record<string, unknown[]>` map plus a `from = jest.fn((t) => ({...chain, then: r => r({data: tableData[t] ?? [], error: null})}))` lets each test set canned data per table and lets `expect(from.mock.calls.map(c => c[0]))` directly verify the no-N+1 invariant. Much cleaner than chaining `mockImplementationOnce` per call.
- React Query keys for hooks that take an array argument should serialize the array into the key (`propertyIds.join(',')`) — TanStack uses structural equality on the key, but a stable string segment is more predictable across re-renders and cheaper to hash. Pattern: `['activityFeed', propertyIds.join(','), limit]`.

## 2026-05-02 23:50 EDT - US-001
- Added 5 light-surface tokens to `unit/tailwind.config.js` under `theme.extend.colors.brand`: `cloud:'#F4F5F7'`, `mist:'#FFFFFF'`, `paper:'#E5E7EB'`, `ink:'#101B29'`, `'ink-muted':'#465A75'`. Existing dark tokens left untouched.
- Documented the new tokens, hex codes, and "permitted only on Home Feed and migrated screens; headers and tab bar stay brand-navy" rule in the Brand Theming section of `unit/CLAUDE.md`. Added a callout that `text-brand-gray` on `bg-brand-mist` is BANNED (fails AA) and that US-016 will add an additive lint rule for that pairing.
- Files changed: `unit/tailwind.config.js`, `unit/CLAUDE.md`, `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`).
- Visual check: purely additive — no JSX touched, so no on-screen pixels changed. iOS-simulator visual diff is by inspection only (acceptable for foundation story with no consumers yet).
- **Learnings for future iterations:**
  - The brand-lint deny-list is whitespace-sensitive bash regex. Future stories that introduce new tokens should add positively-named variants (no `gray-`/`white`/`black` substrings) so they pass automatically.
  - When semantic-only aliases share a hex with an existing token (`brand-ink`==`brand-navy`, `brand-ink-muted`==`brand-blue`), call that out in CLAUDE.md so devs don't think the palette grew.
  - `unit/CLAUDE.md` Brand Theming section is the canonical source — keep tailwind.config.js and that section in lockstep.
---

## 2026-05-03 00:15 EDT - US-002
- Extended `Promotion` type in `unit/services/promotions.ts` with the four columns added by `20260501000003_external_promotions.sql`: `created_by_admin_id`, `external_contact_name`, `external_contact_email`, `external_contact_phone` (all `string | null`). `advertiser_id` was already typed `string | null`, so no change needed there.
- Added exported `PromotionKind = 'tenant' | 'external'` union and `getPromotionKind(promotion)` helper next to the `Promotion` type. Helper is `Pick`-typed so it works on partial selects. Did NOT add `kind` as a field on the row type — Supabase `select('*')` would never populate it and the lying shape would bite later.
- Extended `Property` type in `unit/services/properties.ts` with `latitude: number | null` and `longitude: number | null` per `20260501000002_property_clusters.sql`. CRITICAL: also updated `PROPERTY_COLUMNS` constant — without that, the new fields would silently come back `undefined` at runtime even though TS thinks they exist.
- Added new `unit/services/promotionLeads.ts` exporting `PromotionLeadStage` (six-value union mirroring SQL enum) and `PromotionLead` row type. No service functions yet — those land in US-015 per PRD ("table is intentionally unused at rest until V2").
- Added new `unit/services/propertyGroups.ts` exporting `PropertyGroup` and `PropertyGroupMember` row types. Not consumed in this milestone (PRD chose radius-based auto-grouping) but unblocks future labelled-cluster UI.
- Updated single caller in `unit/app/(admin)/properties.tsx` line 65 to pass `latitude: null, longitude: null` on property create. Chose explicit nulls over loosening `Omit<Property,'id'|'created_at'>` so US-003 geocoding can fill them in without changing the create signature again.
- Files changed: `unit/services/promotions.ts`, `unit/services/properties.ts`, `unit/services/promotionLeads.ts` (new), `unit/services/propertyGroups.ts` (new), `unit/app/(admin)/properties.tsx`, `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`).
- Visual check: zero JSX touched (one TS-only edit to admin properties.tsx adding two `null` fields to an object literal). No on-screen pixels changed; iOS-simulator visual diff not required for foundation type story.
- **Learnings for future iterations:**
  - The `*_COLUMNS` string constant in each service file is the silent failure mode — extending a row type without updating the SELECT list compiles fine but returns `undefined` at runtime. Grep for `_COLUMNS` whenever you touch a row type.
  - When a service exposes `create(Omit<Row,'id'|'created_at'>)`, every required nullable column you add forces a callsite update. Prefer passing `null` over weakening the Omit — the type tells the truth and the next geocoding story can set the value without another signature change.
  - Discriminator unions (`kind: 'a' | 'b'`) for DB rows are best modelled as exported helpers (`getXKind(row)`) over a `Pick<>` of the discriminator column, NOT as an extra field on the row type. Supabase won't populate computed fields and the row type stays honest about the wire shape.
  - For V2-scaffold tables that are explicitly "unused at rest" (per migration comment), still ship the row types early — it lets later stories reference real types instead of `any`, and the marginal cost is one tiny file.
---

## 2026-05-03 00:35 EDT - US-003
- Added `unit/services/geocoding.ts` exporting `geocodeAddress(address)` backed by OSM Nominatim. 1 req/sec throttle via a module-local `lastRequestAt` + setTimeout; required `User-Agent: UNIT-app/1.0 (https://unit.app)` header per Nominatim policy. Validates lat/lon are finite and within range. Never throws — returns `null` on network/HTTP/parse failure with a `console.warn` so callers can persist NULL coords and retry later.
- Added `propertiesService.updateCoordinates(id, lat, lon)` and a standalone `updatePropertyCoordinates()` named export in `unit/services/properties.ts` (per US-003 acceptance wording). Update returns the full row using `PROPERTY_COLUMNS` so the new lat/lon round-trips correctly.
- Wired geocoding into `unit/app/(admin)/properties.tsx` admin-create flow: geocode `${address}, ${city}, ${state}` BEFORE insert, pass `coords?.lat ?? null` / `coords?.lon ?? null` to `propertiesService.create()`. Failure is non-blocking — property still saves with NULL coords plus a `console.warn` so the backfill script can retry later. The +1s of inline latency on Add Property is acceptable since admin property creation is a low-frequency action.
- Added `unit/scripts/backfill-property-coordinates.ts` (run via `tsx`). Selects properties where `latitude IS NULL OR longitude IS NULL` using `.or('latitude.is.null,longitude.is.null')`, geocodes each, updates the row. Idempotent — safe to re-run. Uses `SUPABASE_SERVICE_ROLE_KEY` (not the anon key) to bypass RLS since landlord-admin RLS would otherwise block batch updates across multiple admins' properties. Logs per-row status and a summary line.
- Files changed: `unit/services/geocoding.ts` (new), `unit/services/properties.ts`, `unit/app/(admin)/properties.tsx`, `unit/scripts/backfill-property-coordinates.ts` (new), `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`).
- Visual check: only JSX touched is admin properties.tsx (added an `await` before the existing `propertiesService.create(...)` call — no on-screen layout change). Admin "Add Property" still shows the same form and same toast on success.
- Backfill script NOT auto-run against dev — requires service-role key plus network access; per the PRD acceptance, the operator runs it manually after merging.
- **Learnings for future iterations:**
  - Nominatim's 1 req/sec policy is per-user-agent and aggregated globally — a process-local throttle (single `lastRequestAt` timestamp) is the simplest correct guard. Multiple parallel callers in one process will queue automatically since each `await throttle()` updates the timestamp.
  - Make external-API helpers like `geocodeAddress` non-throwing. Geocoding failure is normal (bad address, transient outage) and should never block a property save.
  - Standalone tsx scripts under `unit/scripts/` cannot reuse `services/supabase.ts` (pulls AsyncStorage). Always construct a fresh `createClient` inside the script with `auth: { persistSession: false, autoRefreshToken: false }` and the service-role key.
  - For idempotent SQL backfill filters, `.or('col.is.null,col2.is.null')` is the PostgREST syntax — comma-separated, no spaces.
---

## 2026-05-03 00:55 EDT - US-004
- Added `unit/services/nearbyProperties.ts` exporting `haversineMiles(a, b)` (pure great-circle distance in miles using Earth radius 3958.7613 mi) and `getNearbyPropertyIds(originPropertyId, radiusMiles=2)` (async, returns string[]).
- Implementation choice: client-side haversine over a SECURITY DEFINER `nearby_property_ids` RPC. Documented rationale in the file header. No migration, no `cube`/`earthdistance` extension provisioning required. Property count is small (target MVP < 100 rows) so the full-table scan is fine. Public API is stable enough that a future swap to `supabase.rpc('nearby_property_ids', ...)` is a drop-in replacement when row counts justify it.
- Behaviour spec: origin row is fetched first via `.eq('id', originPropertyId).single()`. If origin coords are null, returns `[origin.id]` alone (no distance computation possible — caller still gets the My-Property feed). Otherwise scans `properties` with `.select('id, latitude, longitude')`, computes haversine to each non-null-coord row, filters to `distance <= radiusMiles`, sorts ascending, and returns `[origin.id, ...neighbours]`.
- Added `unit/hooks/useNearbyPropertyIds.ts` — React Query wrapper. Key `['nearby-properties', originPropertyId, radiusMiles]`. `staleTime` 1 hour (per PRD). `enabled: !!originPropertyId` so the hook can be called unconditionally with a possibly-null origin during render (idiomatic with `useAuth().propertyIds[0]`).
- Added `unit/__tests__/services/nearbyProperties.test.ts` covering 7 cases:
  - haversine identity (0 mi)
  - Daytona cluster: 29.2108,-81.0228 → 29.2208,-81.0228 ≈ 0.69 mi (within 2 mi radius)
  - 1° latitude ≈ 69 mi (well outside 2 mi)
  - NYC → LA ≈ 2,450 mi (sanity)
  - getNearbyPropertyIds returns origin first + neighbours sorted nearest-first, excluding rows outside the radius
  - getNearbyPropertyIds returns only `[origin]` when origin lat/lon are null
  - getNearbyPropertyIds skips rows with null coords during neighbour computation
- Files changed: `unit/services/nearbyProperties.ts` (new), `unit/hooks/useNearbyPropertyIds.ts` (new), `unit/__tests__/services/nearbyProperties.test.ts` (new), `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm test` 12 suites / 59 tests all green; `npm run brand-lint` exits 0.
- Visual check: zero JSX touched. No on-screen pixels changed; iOS-simulator visual diff not required for service+hook foundation story.
- **Learnings for future iterations:**
  - When a service hits the same table twice (one `.eq().single()` + one list `.select()`), mock `select` per-call: `mockImplementationOnce(() => mockChain)` then `mockImplementationOnce(() => Promise.resolve({data, error}))`. The awaited list path resolves directly off `select()` since there's no `.single()` in the chain.
  - Earth radius constant: 3958.7613 mi is the WGS84-mean value; using a single constant keeps tests deterministic. Don't switch units (km vs mi) inside the function — pick one externally and convert at the call site.
  - Returning the origin as element 0 unconditionally (even with null coords) is the right contract: callers can treat the array as `[origin, ...neighbours]` without a separate "did I get origin?" branch. `[origin].length === 1` doubles as "no neighbours" indication.
  - Keeping the public API of `getNearbyPropertyIds` as `Promise<string[]>` (not row objects) means a future RPC swap is invisible to consumers — the wire shape matches `setof uuid` if it ever moves to PG.
---

## 2026-05-03 00:30 EDT - US-005
- Added `unit/app/(tabs)/home.tsx` placeholder screen — `bg-brand-navy` root with `GradientHeader` (so the navy gradient still anchors the top), centred 'Welcome' headline (`font-lora-semibold text-3xl text-brand-gray`), and 'Your activity feed is coming soon.' subtitle (`font-nunito text-base text-brand-steel`). Real activity feed lands in US-007. All four font classes and four type sizes used here are on the brand-contract whitelist; no banned tokens.
- Updated `unit/app/(tabs)/_layout.tsx`: imported `Home` from `lucide-react-native`; added `<Tabs.Screen name="home">` as the FIRST screen with `href: isAdmin ? null : undefined` so the tab is tenant-only (admins keep their existing landing). Final tab order: Home, Directory, Promotions, Community, Alerts (= notifications), Profile.
- Updated post-login redirect in `unit/app/_layout.tsx` (the "stranded-in-auth-group" else branch, lines 75–92 per PRD reality note) — tenants now `router.replace('/(tabs)/home')` instead of `/(tabs)/directory`. Admins still `router.replace('/(admin)/')`. The reset-password and onboarding branches are unchanged.
- Files changed: `unit/app/(tabs)/home.tsx` (new), `unit/app/(tabs)/_layout.tsx`, `unit/app/_layout.tsx`, `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`); `npm test` 12 suites / 59 tests all green.
- Visual check: by inspection — only one new tab inserted at index 0; no existing tab screen JSX touched. Maestro flow verifying default-landing behaviour will be added in US-021.
- **Learnings for future iterations:**
  - `href: isAdmin ? null : undefined` is the canonical pattern for tenant-only tabs in this codebase (already used on Profile). Use it instead of conditionally rendering the `<Tabs.Screen>` — Expo Router's reconciler is happier with a stable tab tree.
  - The post-login redirect lives at `unit/app/_layout.tsx` AuthGuard (NOT `lib/AuthContext.tsx`). The "stranded-in-auth-group" branch is the only place that picks the post-login landing route — change it once and every signup/login/reset flow follows.
  - Adding a new tab does NOT require any change to `useAuth().propertyIds` plumbing; the screen reads from `useAuth()` directly like every other tab.
  - Placeholder screens that will be replaced later should still satisfy the full brand contract (font class + type size on every Text, root `bg-brand-navy`, no banned tokens) so brand-lint stays green every iteration and the placeholder is shippable on its own.
---

## 2026-05-03 01:25 EDT - US-006
- Added `unit/services/activityFeed.ts` exporting `ActivityFeedKind` (six-value union: `promotion | post | alert | tenant_signup | business_update | announcement`), `ActivityFeedItem` row type with discriminator + `occurredAt` ISO string + optional `imageUrl` and `ctaRoute`, and `getActivityFeed(propertyIds, limit=50)`.
- Four parallel sources via `Promise.all`, each fetched exactly once with `.in('property_id', propertyIds).gte(<timestamp_col>, sinceIso)`:
  - `promotions` filtered by `review_status='approved'` and `created_at > NOW()-30d` → `kind='promotion'` (CTA `/(tabs)/promotions/{id}`)
  - `posts` filtered by `created_date > NOW()-30d` → `kind='announcement'` for `type='announcement'` (no CTA), `kind='post'` for event/offer (CTA `/(tabs)/community`)
  - `businesses` filtered by `created_at > NOW()-30d` → `kind='tenant_signup'` (CTA `/(tabs)/directory/{id}`); subtitle prefers category over description
  - `notifications` for the calling user (`.eq('user_id', userId).in('property_id', propertyIds)`) → `kind='alert'` (CTA `/(tabs)/notifications`)
- `kind: 'business_update'` is reserved on the union but not emitted in v1 — `businesses` table has no `updated_at` column. Documented in service header so future stories can wire it up.
- User id resolved via `supabase.auth.getUser()` BEFORE the parallel batch (one extra await; auth is locally cached so latency is negligible). When no user is signed in (e.g. server-side or post-signout race), notifications fetch is skipped entirely — service still returns the other three sources.
- Empty `propertyIds` short-circuits to `[]` BEFORE any DB call. Verified by jest: `expect(mockFrom).not.toHaveBeenCalled()`.
- Merge: `results.flat().sort((a,b) => b.occurredAt.localeCompare(a.occurredAt)).slice(0, limit)`. ISO 8601 strings sort lexicographically as DESC, no Date construction needed.
- Added `unit/hooks/useActivityFeed.ts` — React Query wrapper with key `['activityFeed', propertyIds.join(','), limit]`, `staleTime: 60_000` (1 min per PRD), `enabled: propertyIds.length > 0` so the hook is safe to call unconditionally during render with an empty array.
- Added `unit/__tests__/services/activityFeed.test.ts` (6 cases):
  - empty propertyIds → `[]` and zero `from()` calls (no N+1 short-circuit verified)
  - all four tables queried exactly once each (no N+1 invariant)
  - no signed-in user → notifications source skipped, other three still fetched
  - merge across sources sorted by `occurredAt` DESC with mixed kinds preserved
  - `limit` parameter applied AFTER merge+sort (returns top-N newest globally, not top-N per source)
  - post `type=announcement` → `kind='announcement'` (no CTA); `type=event|offer` → `kind='post'` (with CTA)
- Files changed: `unit/services/activityFeed.ts` (new), `unit/hooks/useActivityFeed.ts` (new), `unit/__tests__/services/activityFeed.test.ts` (new), `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm test` 13 suites / 65 tests all green; `npm run brand-lint` exits 0 (`brand-lint-clean`).
- Visual check: zero JSX touched. Service+hook+tests only — Home Feed UI consumption lands in US-007.
- **Learnings for future iterations:**
  - For aggregator services that fan out to 3+ tables in parallel, prefer a per-table thenable mock keyed off the `from(table)` argument over chaining `mockImplementationOnce` per call. Pattern: shared `tableData: Record<string, unknown[]>` + `from = jest.fn((t) => ({...chain, then: r => r({data: tableData[t] ?? [], error: null})}))`. Each test sets canned data per table, and `expect(from.mock.calls.map(c => c[0]))` directly proves the no-N+1 invariant in one assertion.
  - ISO 8601 timestamp strings sort lexicographically — `b.occurredAt.localeCompare(a.occurredAt)` gives DESC order without ever constructing a Date. Faster, allocation-free, and avoids time-zone surprises.
  - `supabase.auth.getUser()` is locally cached and near-instant in normal operation — fine to await before a `Promise.all` batch when downstream queries depend on the user id. Don't try to fold it into the parallel batch with a follow-up await; the resulting code is hard to read for negligible perf gain.
  - When a discriminator union has values that are SCAFFOLDED for future use (`business_update` here), document in the service header WHY they're absent in v1 (no `updated_at` column on the source table) so future readers don't add a redundant fetch trying to populate them.
  - React Query `enabled: propertyIds.length > 0` plus the service's `if (propertyIds.length === 0) return []` short-circuit gives belt-and-suspenders protection: callers can pass an empty array during render without triggering a useless query, and the service still returns sane data if called directly.
---

## 2026-05-03 - US-007
- Created `unit/components/tenant/ActivityFeedCard.tsx` — handles all 6 ActivityFeedKind variants. Promotion cards get a full-bleed 16:9 hero image at the card top (outside padding); all others show a kind indicator row (icon + label). Card base: `bg-brand-mist border border-brand-paper rounded-2xl overflow-hidden`. Tappable cards are `Pressable` with `minHeight:88`. CTA renders only when `onPress` is set AND the kind has a non-empty label in `CTA_LABELS` — announcements get no CTA by having an empty string.
- Created `unit/app/(tabs)/promotions/[id].tsx` — minimal tenant-facing promotion detail screen using the standard dark brand style (bg-brand-navy, GradientHeader + back button, 16:9 image if present, Card with headline/description/dates, optional CTA Button). Required because `activityFeed.ts` emits `ctaRoute: '/(tabs)/promotions/${id}'` and there was no matching file-based route.
- Registered `promotions/[id]` in `unit/app/(tabs)/_layout.tsx` as `<Tabs.Screen name="promotions/[id]" options={{ href: null }} />` so Expo Router hides it from the tab bar while still routing to it.
- Updated `unit/app/(tabs)/home.tsx`: replaced the US-005 placeholder with the real FlatList screen. Screen background `bg-brand-cloud`; GradientHeader keeps navy gradient at top. Loading state shows 3 inline SkeletonCard views (gray rectangle fills on white bg-brand-mist cards) instead of the full-screen LoadingScreen so the light surface context is preserved. Empty state is an inline `HomeFeedEmpty` component (uses `text-brand-ink`/`text-brand-ink-muted`) — the generic `EmptyState` component uses `text-brand-gray` which fails AA contrast on `bg-brand-cloud`/`bg-brand-mist`. Navigation from the home screen uses `router.push(item.ctaRoute as Parameters<typeof router.push>[0])` since ctaRoutes are controlled by the service layer (not user input).
- Files changed: `unit/components/tenant/ActivityFeedCard.tsx` (new), `unit/app/(tabs)/promotions/[id].tsx` (new), `unit/app/(tabs)/_layout.tsx`, `unit/app/(tabs)/home.tsx`, `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`); `npm test` 13 suites / 65 tests all green.
- Visual check: by inspection — no existing screen was modified. Home Feed design uses the approved light-surface tokens (brand-cloud / brand-mist / brand-paper / brand-ink / brand-ink-muted) per CLAUDE.md rules. iOS-simulator Maestro verification lands in US-021.
- **Learnings for future iterations:**
  - The generic `EmptyState` component (bg-brand-navy-light icon container + `text-brand-gray` text) is designed exclusively for dark screens. Do NOT reuse it on a light-surface (brand-cloud) screen — it fails AA contrast. Write an inline empty state with `text-brand-ink`/`text-brand-ink-muted` instead.
  - When `activityFeed.ts` emits a `ctaRoute` pointing to a new file-based route (e.g. `/(tabs)/promotions/${id}`), that route file MUST be created and registered in `_layout.tsx` or navigation crashes at runtime. Check that every ctaRoute in `getActivityFeed()` maps to an existing screen before the story ships.
  - For light-surface skeleton cards, use `bg-brand-paper` (#E5E7EB) rectangles on `bg-brand-mist` (#FFFFFF) cards — the contrast is exactly right for skeleton fills without introducing any banned tokens.
  - Inline `style` on Pressable can combine both a function `(state) => ({...})` for press feedback and a fixed value (`minHeight:88`) in the same expression: `style={({ pressed }) => ({ opacity: pressed ? 0.85 : 1, minHeight: 88 })}`. This is established by the existing `Card.tsx` Pressable pattern.
  - `router.push` with a runtime string (not a literal) needs an explicit cast in Expo Router typed-routes mode. The safest cast that stays type-aware is `as Parameters<typeof router.push>[0]` — it extracts the actual `Href` type rather than blindly casting to `never`.
---

## 2026-05-03 - US-008
- Updated `unit/app/(tabs)/home.tsx` to add a SegmentedControl with two options ('My Property' / 'Nearby (≤ 2 mi)') above the FlatList, below the GradientHeader. The control is sticky — it sits outside the FlatList so it's always visible while scrolling.
- Wired `useNearbyPropertyIds(propertyId || null)` (staleTime 1h). `hasNearbyNeighbors = nearbyIds.length > 1` (origin is always element 0, so >1 = at least one real neighbour). `feedIds` computed with `useMemo`: Nearby segment returns the full `nearbyIds` array when neighbours exist; otherwise falls back to `[propertyId]`.
- `handleSegmentChange` is a no-op when the user taps 'Nearby' and `!hasNearbyNeighbors` — the segment stays on 'My Property'. A helper text 'No nearby properties within 2 miles.' is shown below the control whenever `!hasNearbyNeighbors`.
- Segment state is `React.useState` — persists for session only, resets on cold start (per acceptance criteria).
- The SegmentedControl component uses dark tokens (`bg-brand-navy-light`, `bg-brand-blue`) — rendered on the light `bg-brand-cloud` background it appears as a dark pill, which is an intentional contrast element (Delta-inspired). No brand-lint violations.
- Files changed: `unit/app/(tabs)/home.tsx`, `unit/scripts/ralph/prd.json`, `unit/scripts/ralph/progress.txt`.
- Quality gates: `npx tsc --noEmit` exits 0; `npm run brand-lint` exits 0 (`brand-lint-clean`); `npm test` 13 suites / 65 tests all green.
- Visual check: by inspection — the only screen modified was home.tsx. No existing screen was touched. iOS-simulator Maestro verification lands in US-021.
- **Learnings for future iterations:**
  - For a segmented control above a long list, placing the control OUTSIDE the FlatList (between GradientHeader and FlatList) gives a sticky-header effect at zero extra complexity — no need for `stickyHeaderIndices` or `ListHeaderComponent`.
  - `useMemo` for `feedIds` prevents unnecessary React Query re-fetches when the screen re-renders: the memoized array only changes when `segment`, `hasNearbyNeighbors`, `nearbyIds`, or `propertyId` change.
  - When disabling a segment option, the simplest pattern (no SegmentedControl API change) is: guard in `onChange`, make the tap a no-op, and show an explanatory Text below the control. Avoids extending the component for a single use-case.
  - The existing SegmentedControl uses `text-brand-steel` on `bg-brand-navy-light`. Per CLAUDE.md this fails WCAG AA (4.48:1). That's a pre-existing issue in the component that US-018 will address as part of the dark-on-dark audit sweep.
---
